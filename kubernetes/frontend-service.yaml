apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: wanderlust-frontend
  namespace: wanderlust-ns
spec:
  type: NodePort
  selector:
    app: wanderlust-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
      nodePort: 30173
